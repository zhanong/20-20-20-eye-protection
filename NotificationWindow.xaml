<Window x:Class="Eye202020.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="White"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight">
    <Border x:Name="NotificationBorder"
            Background="White"
            CornerRadius="8"
            Padding="20,15"
            BorderThickness="1"
            BorderBrush="#E0E0E0">
        <Border.Effect>
            <DropShadowEffect Color="#40000000" 
                              Direction="270" 
                              ShadowDepth="4" 
                              BlurRadius="8" 
                              Opacity="0.3"/>
        </Border.Effect>
        <StackPanel>
            <Image x:Name="InstructionImage"
                   Source="pack://application:,,,/Images/Instruction.png"
                   Stretch="Uniform"
                   MaxWidth="400"
                   MaxHeight="300"
                   Margin="0,0,0,10"
                   Visibility="Collapsed"/>
            <TextBlock x:Name="CountdownText"
                       Text="Look away for 20 seconds"
                       Foreground="Black"
                       FontSize="16"
                       FontWeight="SemiBold"
                       TextAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>

