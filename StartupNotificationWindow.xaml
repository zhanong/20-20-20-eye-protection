<Window x:Class="Eye202020.StartupNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight">
    <Border x:Name="NotificationBorder"
            Background="#F2F2F2"
            CornerRadius="8"
            Padding="16,12"
            BorderThickness="1"
            BorderBrush="#E0E0E0">
        <Border.Effect>
            <DropShadowEffect Color="#40000000" 
                              Direction="270" 
                              ShadowDepth="4" 
                              BlurRadius="8" 
                              Opacity="0.3"/>
        </Border.Effect>
        <TextBlock Text="20-20-20 Eye Protection started"
                   Foreground="#333333"
                   FontSize="13"
                   FontWeight="Normal"
                   TextAlignment="Center"/>
    </Border>
</Window>

